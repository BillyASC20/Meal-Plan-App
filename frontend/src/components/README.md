This folder contains small UI and utility components.

- GlassCard.tsx, GlassButton.tsx: UI components used by pages
- api.ts: Centralized helper for backend calls (detect-ingredients, generate-recipes). Uses http://localhost:5000 by default.
